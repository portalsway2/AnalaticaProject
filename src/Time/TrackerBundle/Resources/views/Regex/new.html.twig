{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Regex creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('regex') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
