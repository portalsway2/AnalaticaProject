{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Regex list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Regex</th>
                <th>Version</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('regex_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.regex }}</td>
                <td>{{ entity.version }}</td>
                <td>{{ entity.name }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('regex_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('regex_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('regex_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
